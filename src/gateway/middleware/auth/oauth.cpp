/**
 * @file oauth.cpp
 * @brief OAuth 2.0 authentication middleware
 */

#include "gateway/middleware/auth/jwt.hpp"

namespace gateway::middleware::auth {

// OAuth implementation would go here
// For now, uses JWT validation as base

} // namespace gateway::middleware::auth
